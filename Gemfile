source 'https://rubygems.org'

group :jekyll_plugins do
  gem 'jekyll'
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jekyll-redirect-from'
  gem 'jekyll-include-cache' # Add this
  gem 'jemoji'
  gem 'webrick', '~> 1.8'
end

gem 'github-pages', group: :jekyll_plugins
gem 'minimal-mistakes-jekyll' # Add this to ensure the theme loads correctly
gem 'connection_pool', '2.5.0'
